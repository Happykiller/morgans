# Info Smtp
SMTP_HOST=host.docker.internal
SMTP_PORT=1025

# Info mail
MAIL_FROM=contact@happykiller.net

# Redis
REDIS_URL=redis://host.docker.internal:6379

# Bulk throttling
SMTP_THROTTLE_MAX_ITERATIONS=15
SMTP_THROTTLE_WINDOW_MINUTES=1

# Logging
LOG_LEVEL=DEBUG
