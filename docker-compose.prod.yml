# docker-compose.prod.yml
services:
  morgans:
    image: morgans
    container_name: morgans
    ports:
      - '8025:8000'
    restart: always
    env_file:
      - .env
    networks:
      - interservices

networks:
  interservices:
    external: true
